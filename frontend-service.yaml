apiVersion: v1
kind: Service
metadata:
  name: playlist-frontend-service
  namespace: raphaelasilva
spec:
  selector:
    app: playlist-recommendor
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 50025
    targetPort: 50025